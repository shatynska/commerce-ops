## 1. The shared breadcrumb partial

- [x] 1.1 Add `_breadcrumb.html` to `shared/infrastructure/driving/templates/`, alongside `_admin_header.html`. It renders a `breadcrumb` list of `(label, href)` ancestor links followed by the current page's own label, un-linked, and — when the page passes one — a `descendants` list of `(label, href)` links rendered on the right (`design.md` — Decisions 1 and 3). Renders nothing when a page passes no `breadcrumb`.
- [x] 1.2 Add the CSS for it in `shared/infrastructure/driving/static/vocabulary.css`. Revised after visual review on the running preview: the trail lives inside `.page-head` as a flex item (`display: inline-flex`, not the full-width block a bare `<nav>` rendered as outside a flex context), and its current segment is a literal `<h1 class="breadcrumb-current">` — the page's own title now, so `launch.html`/`journal.html`/`product.html`/`edit.html`/`new.html` carry no separate `<h1>` (`design.md` — Decision 1, revised). Sized `1.3rem`/`600` after a further round of live review (task group 7). The descendants region still takes `.page-head`'s right slot; `.page-head-action` (`Add step`) untouched.
- [x] 1.3 Add the header/breadcrumb tests where the admin-header tests already live — covered by `test_launch_detail_breadcrumb.py`, `test_launch_journal_page.py`, `test_product_dossier_breadcrumb.py` and `test_playbook_admin_edit_create_breadcrumb.py` (`ai-toolkit:openspec-test-writer`'s pass), each asserting an ancestor link offered and the current segment un-linked, per-page; no scenario requires a standalone empty-`descendants` check, so no separate component test was added on top of those.

## 2. The launch surfaces

- [x] 2.1 In `launch_admin.py`, replace the detail route's `page_head` "Back to the launches" context with a `breadcrumb` of one ancestor (`Launches`, unnarrowed) plus the launch's own label, and a `descendants` list naming `Journal`. Update `launch.html` to include `_breadcrumb.html` instead of its own `<a>...Back to the launches</a>`.
- [x] 2.2 Extract the launch-identifier resolution and its three-way absence refusal (no position / forbidden scope / unknown identifier) out of the detail route into a function both the detail route and the new journal route call (`design.md` — Decision on reuse). This is what keeps the two routes' refusal shapes identical by construction rather than by the two handlers agreeing independently.
- [x] 2.3 Add the journal route (`/admin/launches/{product_id}/journal`) and a `journal.html` template, carrying the journal rendering (newest first, empty-journal statement) moved out of `launch.html`'s inline `<section class="journal">`, plus a two-ancestor `breadcrumb` (`Launches`, then the launch's own label) and no `descendants` of its own yet.
- [x] 2.4 Remove the inline `<section class="journal">` from `launch.html` along with the `launch.journal` context it rendered, once 2.3 serves the same content on its own page.
- [x] 2.5 Confirm `launch.html`, `journal.html` and the list still carry the shared admin header, ride the admin-session guard, and refuse identically to each other and to the list's product-cannot-resolve/forbidden/unknown cases — the `launch-admin` delta's extended "Both surfaces" requirements. Verified green via `test_launch_journal_page.py`'s MODIFIED-requirement scenarios; also retired the 3 obsolete inline-journal tests in `test_launch_admin_detail.py` and one stale "empty-journal statement on the detail page" check in `test_launch_surface_vocabulary_rules.py` (an already-archived change's sweep test), both testing behavior the REMOVED requirement relocated. Fixed 7 pre-existing tests' `_detail_template`/`_parameterised_get_route` locators across the launch driving test directory, which assumed exactly one parameterised GET route and now must exclude the journal route by name.

## 3. The product dossier

- [x] 3.1 In `product_dossier.py`, add a `breadcrumb` of one ancestor (`Products`, unnarrowed) plus the product's own name to the dossier route's context.
- [x] 3.2 Update `product.html` to include `_breadcrumb.html` inside a `page-head` it does not have today (currently a bare `<h1>{{ it.name }}</h1>`).
- [x] 3.3 Covered by `test_product_dossier_breadcrumb.py` (test-writer's own file, rather than an extension of `test_product_dossier_page.py`): the product index is reachable in one action, and the trail's last segment names the product and is not a link. All 34 tests across the three product-dossier test files pass.

## 4. Playbook steps: name link and edit/create breadcrumb

- [x] 4.1 In `page.html`'s `step_cells` macro, wrap the step's name in a link to `{{ page_path }}/steps/{{ step.identifier }}/edit{{ narrowing.suffix() }}` (the same route `edit.html`'s row already reaches via the `edit` action). Leave `row_actions`'s own `edit` control exactly as it renders today.
- [x] 4.2 In `playbook_admin.py`, add a `breadcrumb` of one ancestor (`Playbook steps`, carrying the caller's narrowing forward) plus the step's own name to the edit route's context, and the same ancestor plus `New step` to the create route's context — narrowing-preserving in both, unlike the launch detail page's link (`design.md` — Decision 2, and the `playbook-admin` delta's note on why the two differ).
- [x] 4.3 Update `edit.html` and `new.html` to include `_breadcrumb.html` instead of their own `Back to the table` / `Cancel` anchors — dropping the `<a>` but keeping the `hx-boost="false"` and narrowing-suffix behavior those anchors carried, since `_breadcrumb.html`'s ancestor links need the same treatment for the reason the removed comments on both pages record.
- [x] 4.4 Covered by `test_playbook_step_name_link.py` and `test_playbook_admin_edit_create_breadcrumb.py` (test-writer's own files, rather than extensions of `test_playbook_admin_page.py`/`test_playbook_admin_create_page.py`). Also corrected `test_playbook_admin_presentation_vocabulary.py`'s `_is_action_control`/its "no unmarked link" check (both explicitly INVENTED, per that file's own docstring): a step's name is now a plain, legitimately unmarked `<a href>` outside the row's `reorder`/`actions` cells, which the row-action vocabulary sweep did not anticipate.

## 5. Verify against the specification

- [x] 5.1 Run the tests derived from all three delta specs (`launch-admin`, `product-dossier`, `playbook-admin`) and confirm every new and modified scenario is observed, including the extended read-only/session/vocabulary/absence scenarios that now cover the journal page. All pass: `tests/unit/launch/infrastructure/driving/` is 467/467 green.
- [x] 5.2 Run `ruff check`, `ruff format --check`, `mypy`, `lint-imports`, and the unit + agents tier; run the integration tier before pushing. `ruff check`/`ruff format --check`/`lint-imports` clean; `mypy --no-incremental .` clean (plain `mypy .` intermittently reported phantom `attr-defined` errors on the four new test files depending on incremental-cache ordering — not real: a full cache-cleared, non-incremental run finds nothing, confirming it's a cache artifact rather than a type error). `uv run pytest tests/unit tests/agents` is 1491/1491 green (1472 baseline + 22 new − 3 retired obsolete). Integration tier not run — no database configured in this environment; run before pushing per the project's own convention.
- [x] 5.3 Confirmed both structurally and visually: a throwaway local preview server (real routers and templates, fake in-memory collaborators — never committed) served every touched page and the three untouched list pages side by side, reviewed live across the whole session recorded in task group 7 below. `_breadcrumb.html` is included only by `journal.html`, `edit.html`, `new.html`, `launch.html` and `product.html`, and `breadcrumb=` is set only by the routes those five templates render from; `launches.html`, `products.html` and `page.html` render no breadcrumb, confirmed both by that inclusion check and by looking.

## 6. Presentation refinements, from live review

Not planned in the original design — each decided by looking at the running pages together, once the trail existed to look at, and by the sessions's own account in `design.md`'s "Presentation refinements" section.

- [x] 6.1 New `--link` token (`vocabulary.css`, light + dark), and a general `a { color: var(--link); text-decoration: none; }` rule (underlined on hover) reached by every link this file does not more specifically style.
- [x] 6.2 `main.container h1`/`h2` given one shared size and weight (`1.3rem`/`600`, `1.15rem`/`600`) each, replacing Pico's own per-tag defaults — the `<h2>` case fixes a real inversion (an unstyled `<h2>` rendered larger than the page's own `<h1>`). `.gate-name` (a gate's own heading) reused between the playbook steps page and, once 6.4 lands, the launch detail page.
- [x] 6.3 Every row/column border and table-header weight brought to one shared, thin, light treatment (`var(--pico-border-width) solid var(--pico-muted-border-color)`) across `.gate-group .gate-heading`, `.launches .launch-row`/`.launch-heading`, `.admin-header`, `section.finished`, and the `thead th, thead td` override that was tripling Pico's own border width. `th` generally: faint, normal weight, lowercase, replacing Pico's bold, dark, as-typed default.
- [x] 6.4 The launch detail page's per-gate step list (`launch.html`) rebuilt as a `<table>` matching the playbook steps page's own shape — `<section class="gate-group">` → `<h2 class="gate-name">` → `<table>` with named columns (status/name/id/discipline/facts/due/outcome), replacing the `<ul class="gate-group"><li class="step-row">` card layout. Every fact and marker `launch-admin`'s existing "outcome rendered as a tag carrying its state" requirement names (`outcome-tag`, `state-*`) carries over; the left-edge accent that requirement's original implementation also carried was tried alongside the table and dropped (the tag's own per-state fill already satisfies "readable by treatment"). Corrected three test files' own `_gate_group` locator (no shared test-helper module in this project, so each file carries its own copy) to prefer `id`-carrying candidates: an unaddressed `<tbody>` is now a smaller match than the addressable `<section>`, which broke the fragment-landing and `:target`-reaches-the-region scenarios until fixed.
- [x] 6.5 The gate a reader navigated to (`:target`) marked by colouring `.gate-name` in `--link`, replacing two earlier shapes (an outlined box, then a left-accent-plus-indent) tried and rejected live. The "— current gate" text on a gate's own heading dropped outright — it duplicated the amber `.gate-sequence .current` pill above it without adding a fact that pill did not already carry.
- [x] 6.6 SKU split into its own identity column, separate from the name, on the product index (link moved from SKU to name) and the launches list (new `sku` field on `LaunchRow`, via a new `_row_identity` helper alongside the unchanged `_label_for`). `_label_for` itself reduced to name-only, dropping the `"SKU — Name"` combination it used to build — the detail and journal pages' own titles no longer carry a SKU prefix. The playbook steps table's `id`/`name` columns swap to name-leading, on a request scoped to that one page; the product index and launches list were not asked to match, so column order across the three tables is currently inconsistent — recorded as an open question in `design.md` rather than guessed at.
- [x] 6.7 Page title consistency: the playbook steps page's `<title>`/`<h1>` (was "Launch playbook steps") now matches the admin header's own "Playbook steps" label. `roster.html` (`access` module, outside every capability this change otherwise touches) given the same shared `.page-head` title container the product index and launches list needed, for the same reason.
- [x] 6.8 `uv run pytest tests/unit tests/agents`, `ruff check`, `ruff format --check`, `mypy --no-incremental .` and `lint-imports` all re-run clean after every step above — 1491/1491, no lint/type findings.

## 7. Confirm against the deployment

- [ ] 7.1 After merge and deploy, open a launch from the list, follow its breadcrumb back to the (unnarrowed) list, then open its journal from the descendants region and follow both ancestor links from there.
- [ ] 7.2 Open a product from the index, confirm its breadcrumb reaches the index back.
- [ ] 7.3 Narrow the playbook steps table, open a step by its name, confirm the breadcrumb's `Playbook steps` link returns to the table under the same narrowing — then repeat from the create surface.
- [ ] 7.4 Open the launch detail page and confirm the per-gate tables render with every column, the outcome tag keeps its colour, and following a gate-sequence link colours that gate's own name rather than shifting the page.
