> Symbols are named without line numbers throughout: every file this
> change touches is rewritten by `redesign-step-fields`, so a line
> number recorded now is wrong by the time the task is worked
> (design.md — Context).

## 1. Sequencing prerequisite

- [x] 1.1 Confirm `redesign-step-fields` has merged to `main` before starting any other section. It rebuilds the step's field set across all four layers and rewrites the create form this change relocates; drafting against today's fields produces a page that has to be rewritten on arrival (design.md — Sequencing). Merging is not archiving — the spec-level hazard is guarded at 6.6
- [x] 1.2 Rebase this branch on `main` and re-read `_fields.html`, `page.html`, `playbook_admin.py` and `_submitted_values` as that change left them. This change carries no code, so the rebase itself is clean; what it does not do is tell you what moved
- [x] 1.3 Confirm the `## MODIFIED` block for *Steps can be created, retired and un-retired from the page* still reproduces that requirement as it stands in `openspec/specs/playbook-admin/spec.md`. `redesign-step-fields` does not carry a block for this header, so it should be unmoved — verify rather than assume, and reconcile if it moved. **One difference is deliberate**: the scenario *A blocked retirement explains itself* ends "the step is not retired" where the served text says "the step remains live". It is disclosed in `proposal.md` and is not a drift to reconcile away; every *other* difference is
- [x] 1.4 Confirm the same for *The narrowed view survives every write and every move between views*, whose text this change reproduces from `reorder-steps-under-filters` (archived). **One difference is deliberate here too**: "the description search" reads "the text search", matching the term `redesign-step-fields` itself adopts once search covers a step's name as well. Also disclosed in `proposal.md`
- [x] 1.5 Confirm the three requirements `redesign-step-fields` landed that this change's scenarios lean on are present in the served specs, and that this change's wording still matches theirs: *The step form carries every authorable field* (the hidden-or-disabled rule), *Steps that are not active are visible to authors and set apart* (where a non-active step renders), and *The step table shows the live set whole* (search matching name as well as description)

## 2. Tests derived from the delta specs

- [x] 2.1 Dispatch `ai-toolkit:openspec-test-writer` scoped to the scenarios this change **introduces or changes** — the creation requirement's, including *A created step appears in its gate*, whose `THEN` this change narrows to an `active` create and extends with the addressing clause; the two create-surface narrowing scenarios; and the anchor requirement's four. Exclude *A blocked retirement explains itself*, reproduced with only the "live" wording retired, and the five narrowing scenarios belonging to `reorder-steps-under-filters` — those six are already covered by existing tests
- [x] 2.2 Record the baseline: run `uv run pytest` and confirm the new tests fail for the stated reason, not for a missing import or fixture

## 3. The create surface

- [x] 3.1 Hoist `_DISCIPLINE_OPTIONS: Final = [d.value for d in Discipline]` to module scope and have `_option_context()` use it — today `discipline_options` exists only as a dict key, so `_submitted_values` cannot refer to it
- [x] 3.2 Add `"discipline": form.get("discipline") or _DISCIPLINE_OPTIONS[0]` to `_submitted_values` — without the key a rejection reverts the discipline and the retry generates an identifier `update_step` will refuse to correct. Not a `""` default: an empty string matches no option and renders nothing selected. `redesign-step-fields` keeps both underlying rules, so it does not close this
- [x] 3.2a Make the discipline field required and refuse a create submitting none, resolving the disagreement between the create route's `Discipline.LISTING` fallback and the template's first option (`strategy`) — the select always submits a value, so only a hand-built POST omits it. The delta states this as a clause and a scenario, so 2.1 derives a test for it
- [x] 3.2b ~~Fix the multi-valued round-trip~~ — **`redesign-step-fields` did it after all.** Verified on `main` after the rebase: `_form_of` now returns `tuple[dict[str, str], tuple[str, ...]]`, reading assignees with `posted.getlist("assignees")`, and `_submitted_values(form, assignees)` echoes them as a list. The contingency did not fire; nothing to do here beyond asserting the behaviour from the create surface, which the delta's two-assignee scenario already covers
- [x] 3.2c ~~Blast-radius containment for 3.2b~~ — **not needed**, 3.2b having arrived done. Left in place rather than deleted so the review record stays readable
- [x] 3.3 Add `GET {PAGE_PATH}/steps/new` behind `_require_admin`, rendering a new `templates/new.html` that includes `_fields.html` with the create-surface flags `redesign-step-fields` left the partial carrying
- [x] 3.4 Accept the active narrowing on that route and render **Cancel** as a link back to the list carrying it
- [x] 3.5 Carry the active narrowing on `POST {PAGE_PATH}/steps/create` too — via the create form's `action` query string — and use it for the rejection re-render's **Cancel** link and for the success redirect
- [x] 3.6 Put `hx-boost="false"` on the **Add step** control, on the create form in `new.html`, and on **Cancel** — so no transition depends on an attribute inherited from a page being replaced. Setting it on `new.html`'s `<body>` instead would not work: while **Add step** is still boosted, the response is swapped *into* `page.html`'s body and the new body's attributes are discarded (design.md — Success redirects)
- [x] 3.7 Change `POST {PAGE_PATH}/steps/create` to re-render `new.html` on an `InvalidPlaybookError`, passing the submitted values alongside the fault list
- [x] 3.8 Do the same on `StaleStepSetError`: the stale notice plus the submitted values, persisting nothing
- [x] 3.9 On a create that lands, return `303` to `{PAGE_PATH}?<narrowing>&created=<identifier>#step-<identifier>`, taking the identifier from the `StepRecord` `create_step` returns. The `created` parameter is what makes 3.11 possible — a fragment is never sent to the server
- [x] 3.10 Give each rendered step row an `id` matching that fragment — **every** row, not only the rows of a gate's active steps, since a step created as a `draft` renders among the non-active steps and must still be addressable. Escape the dots wherever the identifier is used as a CSS selector
- [x] 3.11 Have the list `GET` read `created`, and render the notice only when clearing the narrowing being offered would actually reveal that step — name it as falling outside, and offer the clear. Ask the page's own filter whether the step is revealed; do not reimplement the predicate, which now matches a search term against name *and* description
- [x] 3.12 Have that clear-narrowing offer keep `created` and the fragment, so clearing lands on the created step rather than at the top of the set
- [x] 3.13 Render the list unchanged when `created` is absent, names a step **the page's own read does not return at all**, or names one the offered clear would not reveal — a step retired since it was created is still returned by that read and filtered out by the retired-steps control, so a naive "returned but hidden" test would offer a clear that reveals nothing. Do **not** write this test against the *served* set: under `redesign-step-fields` that word means the `active` steps alone, and a created `draft` is outside it while being exactly the step the notice exists to find (design.md — A created step the narrowing would hide)
- [x] 3.14 Carry `created` forward only on the clear-narrowing offer of 3.12 — the list's own controls carry the narrowing alone, so the notice cannot re-fire on renders that follow an unrelated action
- [x] 3.14a Leave `retired` out of the statuses the create surface offers. **Now concrete**: `_fields.html` renders one `<select name="status">` over `status_options`, which `_option_context()` builds from `_STATUS_LABELS` — all four statuses, `RETIRED` included, defaulting to `draft`. The partial is shared with the edit surface, so filter for the create surface specifically rather than narrowing `status_options` globally — take the same route `include_discipline` already takes through this partial. A step created straight into `retired` would render behind the reveal control, where 3.9's fragment addresses nothing and 3.11's notice cannot help
- [x] 3.14b Refuse a `POST` naming `retired` server-side as well, and refuse a `POST` naming no discipline (3.2a) the same way — without rendering a create surface. Neither request can come from the surface's own controls, so there is no half-typed form to hand back; this is the pattern the reordering requirement already sets, refusing a move submitted where reordering is unavailable rather than trusting the absent control
- [x] 3.15 Confirm nothing in 3.9–3.13 assumes the created step joined its gate's active order: a step created in any status other than `active` holds no slot and renders outside that order (design.md — Where a created step lands). Look the created step up in what the page renders, not in a gate's ordered active steps

## 4. Timing anchor inputs

- [x] 4.1 Wrap each anchor input in `_fields.html` in its own group, so a group can be rendered offered or not offered. Per input, not per anchor kind: `anchor_start` serves two kinds and must not be rendered twice
- [x] 4.2 Render that state server-side from the anchor kind the surface was rendered with — the step's own on a fresh edit, the submitted one around a rejection, the default on a fresh create
- [x] 4.3 Use `hidden`, not `disabled`. `redesign-step-fields` states the general meaningless-field rule as "hidden or disabled", which is right for an automation brief on a `human` step; here the requirement demands the value be *retained and still submitted*, and a disabled input submits nothing (design.md — Anchor inputs)
- [x] 4.4 Add the inline script re-applying the state on `anchor_kind` change, leaving not-offered inputs in the DOM so their values survive. Place it inside the swapped region, with `_fields.html` — a `<head>` script never runs on the edit page, which is reached by a boosted body swap
- [x] 4.5 Confirm a not-offered input's value submitting is still ignored by `_anchor_from_form`, and that the delta's scenario for it asserts the written step rather than the rendered form
- [x] 4.6 Keep `anchor_start` offered under both `window` and `open-ended` — it is the one input two anchor kinds share — and keep the anchor kind selector offered under every kind
- [x] 4.7 Confirm the edit page inherits all of the above, since `_fields.html` is shared
- [x] 4.8 Reconcile with the mechanism `redesign-step-fields` already put in `_fields.html`. **Now concrete**: it chose `disabled`, applied to `automation_brief` and `handler` via `{% if not automated %} disabled{% endif %}`. The anchor groups must use `hidden` instead (4.3), so the partial will carry both — which is correct, not a collision to collapse: a disabled automation field must not reach the write, while a hidden anchor input must. Make the difference legible in the template rather than leaving two spellings of "not offered" side by side with nothing saying why

## 5. The list page

- [x] 5.1 Remove the create section from `page.html`
- [x] 5.2 Add the **Add step** control ahead of the gate tables, linking to the create surface and carrying the active narrowing
- [x] 5.3 Confirm no create form remains within or after the gate tables

## 6. Verification

- [x] 6.1 Run `uv run pytest` and confirm the tests from section 2 now pass, with no previously passing test weakened, skipped or deleted
- [x] 6.2 Confirm `test_a_created_step_appears_in_its_gate` in `tests/unit/launch/infrastructure/driving/test_playbook_admin_page.py` still passes across the `200` → `303` change — `_submit` calls `client.request(...)` without `follow_redirects`, so the TestClient default of `True` applies. Check whether `redesign-step-fields` rewrote or replaced it first
- [x] 6.3 Note that the same test finds the **Add step** control only because `_control(page, contains=("new",))` matches the `/steps/new` route; renaming that route breaks it
- [x] 6.4 Run `ruff check`, `ruff format --check`, `mypy`, and `import-linter` — confirm no new module-boundary violation. In particular, confirm this change added no new dependency on `access`: resolving roster people for the assignee control is `redesign-step-fields`'s cross-module dependency, and this change should inherit it rather than open a second path to it
- [x] 6.5 Exercise the surface by hand: a rejected create keeps every value including the discipline and every named assignee; a step created as a `draft` is landed on where it renders rather than looked for in its gate's order; a create under a search term matching neither name nor description says so rather than looking lost; the anchor offers only its kind's inputs
- [ ] 6.6 Before archiving, confirm `redesign-step-fields` is already archived and that `openspec/specs/playbook-admin/spec.md` carries the three requirements named at 1.5 — archived first, this change's creation requirement asserts where a created step renders in the vocabulary of a status the served specs do not define, and `openspec validate` will not catch it
- [ ] 6.7 After archiving, edit `openspec/specs/playbook-admin/spec.md` directly to correct the capability Purpose, which still says the set is changed "in place" by "inline edit, create". A Purpose is not carried by a delta, so `openspec validate` will not report it. `redesign-step-fields`'s task 7.1 rewrites the same Purpose for a different reason — reconcile with what it left rather than overwriting it
- [x] 6.8 Confirm `git diff` touches no file under `launch/domain/` or `launch/application/`
