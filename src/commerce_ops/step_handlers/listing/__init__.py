"""Step handlers for the `listing` discipline.

The directory name is the first segment of the handler names below it, so
`listing.subcategory_advisor` reads as the path it lives at. That
correspondence is a convention, not a rule the registry enforces — the
registry keys on a free string. See README's Architecture section.
"""
