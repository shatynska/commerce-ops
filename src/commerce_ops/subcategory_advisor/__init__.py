"""The sub-category advisor: the first step handler with judgement in it.

A module of its own, following `omni_agent`'s shape, because one handler's
judgement is not the runtime's contract — the same reason `subcategory-advisor`
is a capability separate from `launch-step-automation`.
"""
